    Var i:integer;
        N:integer;

    Begin
        //conversar com o usuário
        write ('informe um número');
        read(N);

        //mostra a tabuada
        for i:=1 to 10 do
            begin
            write(N,'x',i:2,'=',N*i:2);
            end;
        End.
